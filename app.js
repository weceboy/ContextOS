(() => {'use strict';
const $=id=>document.getElementById(id), KEY='contextos.state.v1';
const ids=['projectRepo','projectDescription','projectDirection','taskRepo','task','projectType','existing','analysis','files','criteria','additional']; const workflowDefaults={inspect:true,targeted:true,plan:true,tests:true,fix:true,focused:true,patterns:true,unrelated:true,stop:true,addTests:false,docs:false,security:false,performance:false,progress:false,planFile:false,summary:true}; let workflow={...workflowDefaults}; let mode='project';
const labels={inspect:'Understand the existing project first',targeted:'Only look at the parts that matter',plan:'Think through the solution before coding',tests:'Check that it works',fix:'Fix problems if something fails',focused:'Keep changes focused',patterns:'Follow existing project patterns',unrelated:"Don't change unrelated functionality",stop:'Stop when the requested result is working',addTests:'Add or improve tests',docs:'Update documentation',security:'Check for security problems',performance:'Check for performance problems',progress:'Keep project progress up to date',planFile:'Keep a plan for the current task',summary:'Give me a concise summary when finished'};
function repoOk(v,required=false){if(!v.trim())return !required;try{let u=new URL(v);return u.protocol==='https:'&&u.hostname==='github.com'&&/^\/[\w.-]+\/[\w.-]+\/?$/.test(u.pathname)}catch{return false}}
function get(name){return document.querySelector(`[name="${name}"]:checked`)?.value}
function state(){const s={mode,workflow,knowledge:get('knowledge'),contextMode:get('contextMode'),speed:document.querySelector('.speed.active')?.dataset.speed||'careful'};ids.forEach(id=>s[id]=$(id).value);return s}
function save(){try{localStorage.setItem(KEY,JSON.stringify(state()));$('saveStatus').textContent='Saved locally'}catch{$('saveStatus').textContent='Local saving is unavailable'}}
function apply(){try{const s=JSON.parse(localStorage.getItem(KEY)||'null');if(!s)return;ids.forEach(id=>$(id).value=s[id]||'');workflow={...workflowDefaults,...(s.workflow||{})}; mode=s.mode||'project';document.querySelector(`[name="knowledge"][value="${s.knowledge||'well'}"]`).checked=true;document.querySelector(`[name="contextMode"][value="${s.contextMode||'known'}"]`).checked=true;showMode(mode);setSpeed(s.speed||'careful')}catch{}}
function showMode(next){mode=next;$('projectView').hidden=next!=='project';$('taskView').hidden=next!=='task';$('resultView').hidden=true;document.querySelectorAll('.mode-btn').forEach(b=>b.classList.toggle('active',b.dataset.mode===next));save()}
function knowledge(){return {well:'The user already knows the project well. Keep explanations concise and focus on evidence.',basics:'The user knows the basics. Explain important findings in clear, beginner-friendly language.',little:'The user knows very little about the project. Explain findings clearly and avoid unnecessary jargon.'}[get('knowledge')]}
function bootstrap(){return `You are preparing an existing software project to be used with an AI coding agent such as OpenCode.\n\nRepository:\n${$('projectRepo').value.trim()}\n\nProject description:\n${$('projectDescription').value.trim()||'[Not provided]'}\n\nPlanned direction:\n${$('projectDirection').value.trim()||'[Not provided]'}\n\nUser knowledge:\n${knowledge()}\n\nYour task is to deeply understand the existing repository and create a persistent project-context layer for future coding agents. Do not redesign the application, invent unsupported requirements, or make arbitrary architectural changes.\n\nFirst inspect and understand: what the application does; current architecture; important modules and components; dependencies; existing functionality and tests; current project state; incomplete functionality; architectural decisions; technical debt or risks; conventions; documentation; and important configuration or infrastructure.\n\nThen create these files in the appropriate repository root location while preserving the repository's existing structure:\n\nAGENTS.md — concise, practical rules for future coding agents: read context, inspect relevant code, search before creating functionality, follow patterns, make the smallest reasonable change, run relevant tests, fix failures caused by changes, avoid unrelated modifications, update memory when appropriate, and stop when acceptance criteria are satisfied.\n\nSPEC.md — observed product purpose, users, features, behaviors, inferred functional requirements, constraints, integrations, and clear acceptance criteria. Clearly distinguish observed facts from assumptions.\n\nPLAN.md — current project-level priorities, incomplete areas, recommended next steps, and dependencies between major tasks. Do not create a detailed feature implementation plan.\n\nPROGRESS.md — concise factual status: implemented, partially implemented, unfinished, known issues, current development state, and recommended next steps.\n\nDECISIONS.md — important technology and architectural decisions. For each, include the decision, reason if known, and repository evidence where appropriate. Do not invent reasons; explicitly say when unknown.\n\nLESSONS.md — useful project-specific lessons, including testing setup, pitfalls, commands, non-obvious conventions, integration requirements, and previous problem areas. Do not add generic programming advice.\n\nIMPORTANT RULES\n- Do not modify existing application code.\n- Preserve the repository's existing structure; do not create or rename source directories merely to fit this system.\n- Preserve valuable existing documentation. If equivalent documentation already exists, update or reference it rather than creating conflicts.\n- Be explicit about uncertainty and do not invent facts.\n\nAfter creating the files, provide a concise summary of what you learned, what files you created, important uncertainties, and recommendations for future coding agents. The final result should leave the repository ready to be opened in OpenCode for future development.`}
function discovery(){return `You are helping me understand an existing software repository before a coding task is implemented.\n\nRepository: ${$('taskRepo').value.trim()||'[Not supplied]'}\nProject type: ${$('projectType').value||'[Not supplied]'}\nIntended task: ${$('task').value.trim()||'[Not supplied]'}\n\nONLY inspect and explain the project. Do not modify files, implement the feature, or redesign anything. Inspect only what is necessary.\n\nReturn exactly these headings:\nPROJECT SUMMARY\nRELEVANT FUNCTIONALITY\nRELEVANT FILES\nEXISTING TESTS\nIMPORTANT DEPENDENCIES\nIMPORTANT CONSTRAINTS\nRECOMMENDED APPROACH`}
function taskPrompt(){const on=Object.keys(workflow).filter(k=>workflow[k]).map(k=>labels[k]);return `## ROLE\nYou are the primary coding agent working on an existing software project.\n\n## PROJECT\nProject type: ${$('projectType').value||'[Not specified]'}\n${$('taskRepo').value.trim()?`Repository reference: ${$('taskRepo').value.trim()}\nUse it only if you can access it; otherwise work from available project files.\n`:''}\n## TASK\n${$('task').value.trim()}\n\n## EXISTING PROJECT CONTEXT\n${get('contextMode')==='discover'&&$('analysis').value.trim()?$('analysis').value.trim():$('existing').value.trim()||'Inspect the relevant existing behavior before implementation.'}\n\n## RELEVANT FILES\n${$('files').value.trim()||'Determine the smallest useful set of files using targeted repository search.'}\n\n## WORKFLOW\n${on.map((x,i)=>`${i+1}. ${x}.`).join('\n')}\n\n## ACCEPTANCE CRITERIA\n${$('criteria').value.trim()||'Define reasonable acceptance criteria from the task and existing behavior, then verify them.'}\n${$('additional').value.trim()?`\n## ADDITIONAL INSTRUCTIONS\n${$('additional').value.trim()}`:''}\n\n## FINAL RESPONSE\nKeep the final response concise: what changed, files changed, tests/checks run, results, and any remaining issues.`}
function output(title,text,steps){$('resultTitle').textContent=title;$('output').value=text;const n=Math.max(1,Math.round(text.length/4));$('sizeLabel').textContent=`Approximate token count · ~${n.toLocaleString()} tokens`;$('workflowExplain').innerHTML=steps?`<h3>What happens next</h3><ol>${steps.map(s=>`<li>${s}</li>`).join('')}</ol>`:'';$('projectView').hidden=true;$('taskView').hidden=true;$('resultView').hidden=false;$('resultView').scrollIntoView({behavior:'smooth'});save()}
async function copy(text){try{await navigator.clipboard.writeText(text)}catch{const t=document.createElement('textarea');t.value=text;document.body.append(t);t.select();document.execCommand('copy');t.remove()}$('copyStatus').hidden=false;setTimeout(()=>$('copyStatus').hidden=true,1600)}
function renderAdvanced(){const root=$('advanced');root.innerHTML='';Object.keys(labels).forEach(k=>{const l=document.createElement('label');l.className='check';l.innerHTML=`<input type="checkbox" data-w="${k}" ${workflow[k]?'checked':''}><span>${labels[k]}</span>`;root.append(l)});root.onchange=e=>{if(e.target.dataset.w){workflow[e.target.dataset.w]=e.target.checked;save()}}}
function setSpeed(speed){document.querySelectorAll('.speed').forEach(b=>b.classList.toggle('active',b.dataset.speed===speed));if(speed==='fast')workflow={...workflowDefaults,inspect:true,targeted:true,plan:false,tests:true,fix:false,focused:true,patterns:true,unrelated:true,stop:true,summary:true};if(speed==='careful')workflow={...workflowDefaults};if(speed==='thorough')workflow={...workflowDefaults,addTests:true,docs:true,security:true,performance:true,progress:true,planFile:true};renderAdvanced();save()}
$('prepareProject').onclick=()=>{const ok=repoOk($('projectRepo').value,true);$('projectRepoError').hidden=ok;if(!ok){$('projectRepo').focus();return}output('Your project-preparation prompt is ready',bootstrap(),['Copy the prompt','Paste it into ChatGPT','Let ChatGPT analyze your repository','Save or download the updated repository','Open it in OpenCode'])};$('prepareTask').onclick=()=>{const ok=$('task').value.trim();$('taskError').hidden=!!ok;if(!ok){$('task').focus();return}output('Your coding-task prompt is ready',taskPrompt(),[])};
document.querySelectorAll('.mode-btn').forEach(b=>b.onclick=()=>showMode(b.dataset.mode));document.querySelectorAll('[name="contextMode"]').forEach(r=>r.onchange=()=>{$('knownWrap').hidden=get('contextMode')==='discover';$('discoverWrap').hidden=get('contextMode')!=='discover';$('discoveryPrompt').value=discovery();save()});$('copyDiscovery').onclick=()=>copy($('discoveryPrompt').value);$('copyOutput').onclick=()=>copy($('output').value);$('edit').onclick=$('back').onclick=()=>showMode(mode);document.querySelectorAll('.speed').forEach(b=>b.onclick=()=>setSpeed(b.dataset.speed));ids.forEach(id=>$(id).addEventListener('input',save));$('clearData').onclick=()=>{localStorage.removeItem(KEY);$('saveStatus').textContent='Saved data cleared'};renderAdvanced();apply();$('discoveryPrompt').value=discovery();
})();